spring:
  application:
    name: auth-service
  neo4j:
    uri: bolt://neo4jdb:7687
    authentication:
      username: neo4j
      password: secret
  cloud:
    config:
      enabled: true
      uri: http://config-service:8081

jwt:
  secret: secret
